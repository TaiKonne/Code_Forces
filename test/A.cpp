#include <bits/stdc++.h>
using namespace std;

int main()
{
    int s;
    cin >> s;
    vector<int> alice;
    vector<int> bob;

    while (s > 0)
    {
        int n1;
        cin >> n1;
        for (int i = 0; i < n1; i++)
        {
            int t;
            cin >> t;
            alice.push_back(t);
        }
        int n2;
        cin >> n2;
        for (int i = 0; i < n2; i++)
        {
            int t;
            cin >> t;
            bob.push_back(t);
        }
        
        
    }
}